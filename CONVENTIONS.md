# 项目编码规范

## 1. 代码风格
- 函数、变量名一律用 `snake_case`
- 类名用 `CamelCase`
- 每个函数必须有 docstring（三引号注释）

## 2. 项目结构
- `src/` 存放源代码
- `tests/` 存放测试文件
- `main.py` 是入口

## 3. 测试要求
- 写完函数立刻写测试
- 测试文件命名: `test_模块名.py`
- 每次修改后都要 `/run pytest`

## 4. AI 工作规则
- 禁止使用 `global` 变量
- 所有用户输入都要验证
- 报错信息要中文明了